CREATE TABLE settings (
    id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    zip INTEGER AS Int NOT NULL
);

find:
SELECT * FROM settings;

insert:
INSERT INTO settings (zip)
    VALUES (?);

update:
REPLACE INTO settings VALUES ?;

delete:
DELETE FROM settings;